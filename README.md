created a smart attendance based dashboard where i have used fastapi as the web framework.Also worked with pydantic for data validation(as in case of email it must be "@").
it uses uvicorn as the server and sqlalchemy as the orm to define a connection.Also added some sql queries for analysis 

